# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
# `.next` também pode ser um SYMLINK para `.next.nosync` (ver abaixo) — a
# regra com barra só casa com diretório, e sem esta linha o link é commitado.
/.next
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
# keep example files so forkers have a template to copy
!.env.local.example
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# --- CB Advogados (customizacoes do fork) ---
# Config local do Claude Code (launch.json p/ subir o dev server, settings locais)
.claude/

# Capturas e logs do Playwright MCP. Sem isto elas poluem o `git status`
# a cada sessao — foi assim que um `channel-select 2.tsx` (copia do Finder)
# passou despercebido e acabou commitado.
.playwright-mcp/

# Pasta de build do Next, fora do iCloud (~/Documents é sincronizado).
# O iCloud ignora nomes terminados em .nosync; `.next` é um atalho para cá.
# Sem isso o fileproviderd sincroniza cada arquivo que o Turbopack reescreve,
# e compilar passa de 20s por rota — já travou o dev server duas vezes.
#
# ⚠️ O arranjo JÁ FOI DESFEITO uma vez (2026-08-04): `.next` voltou a ser
# pasta real, com 1,6 GB dentro do iCloud, e o estrago foram 3.175 cópias de
# conflito — incluindo um `src/types/index 2.ts` velho de 5 horas, que uma
# busca no código devolveu ANTES do arquivo verdadeiro. Se o dev server
# voltar a engasgar, conferir isto primeiro:
#     readlink .next        # tem de responder .next.nosync
#
# ⚠️⚠️ **`node_modules` NÃO entra neste arranjo, e não é esquecimento.**
# Medido em 2026-08-04: com `node_modules` virado symlink para
# `node_modules.nosync`, o `tsc --noEmit` passou de 3s para MAIS DE 7 MINUTOS,
# parado em 0% de CPU — o TypeScript resolve cada módulo pelo caminho real e
# o desvio o mata. O `vitest` continuava rápido, o que despista: o sintoma é
# só do tsc. Desfazer o symlink devolveu os 3s na hora.
# `node_modules` muda uma vez por `npm install`; o `.next` é reescrito a cada
# tecla. É o segundo que precisava sair do iCloud, não o primeiro.
.next.nosync

# Cópias de conflito do iCloud. O projeto vive em ~/Documents, e o iCloud
# duplica arquivo/pasta com sufixo " 2" quando acha que houve conflito — já
# apareceram `.next 2`, `BUILD_ID 2` e `channel-select 2.tsx`. Nenhuma delas
# deve ser commitada, e a `.next 2` chegou a entrar na varredura do Tailwind
# (ver o comentário no topo de `src/app/globals.css`).
* 2
* 2.*
*\ 2

# Temporários da CLI do Supabase (entraram por engano num `git add -A`).
supabase/.temp/
